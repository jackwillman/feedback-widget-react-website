import { PageProps } from "../MainPage";

const Dashboard = function DashboardComponent({ setCurrentPage } : PageProps) {
    return <div>
        <p>Dashboard</p>
    </div>;
};

export default Dashboard;